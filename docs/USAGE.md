# 使用说明

运行示例：

```bash
python examples/demo.py
python -m unittest discover -s tests
python -m video_stego_flow.cli --message "demo payload" --report docs/cli_report.md
```

## 可视化报告

浏览器打开 `docs/visual_report.html`，可以查看指标卡片、实验流程、代码组成和结果说明。
